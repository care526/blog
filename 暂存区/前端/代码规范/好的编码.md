```
let { a } = this  // 解构this中的a属性

function ({
  a, b, c
}) { }

let a = [
  1a, 2asd, 3asd, 4dfsd, 
  5jk, 6, 123, 123, 123
]

const a = (b, c, d) => 
  api
    .post({ n, m })
    .then(res => {})
    .catch(e => {})
    .finally(() => {})

(this.$ELEMENT || {}).size  // 防止一个对象是undefined
```