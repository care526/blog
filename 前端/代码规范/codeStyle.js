import { A, B } from C.js

// 命名
var a = 0
var a = b
var _this = this;
var shopName = 'care'
var care = [1, 2, 3, 4]

// 语句
if (true) {}
for (let i = 0; i < 4; i++) {
  // 两个空格的Tab
}
function name(args) {
  // XXX
}

true ? a = 3 : ''